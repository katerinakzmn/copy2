# Local-only credentials. Copy to .env and fill values locally.
# Never commit the .env file.

OPENAI_API_KEY=

# Optional runtime controls
BENCHMARK_OPENAI_TIMEOUT_SECONDS=90
BENCHMARK_MAX_EPISODE_COST_USD=0.10
